credentials:
  usernames:
    admin:
      email: admin@localhost
      name: Admin
      # bcrypt hash of "changeme" — REPLACE THIS with your own password hash:
      #   python3 -c "import bcrypt,getpass; pw=getpass.getpass(); print(bcrypt.hashpw(pw.encode(), bcrypt.gensalt(12)).decode())"
      password: "$2b$12$KIXTSmCo7GcD0dWpTMfm4.pjDHBzmHoFM7.x5BTYgvB9Tb7PvXxiO"

cookie:
  expiry_days: 7
  key: "trading_dashboard_secret_key_change_me_32chars"
  name: "trading_dashboard_token"
