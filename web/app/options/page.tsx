import OptionsPage from "./OptionsPage";

export const metadata = {
  title: "Options Screener — Oracle",
  description: "Daily US options flow signals: RSI, PCR, IV Rank, unusual activity",
};

export default function Page() {
  return <OptionsPage />;
}
