.env
data/
*.csv
daily_run.log
__pycache__/
*.log
dashboard/config.yaml

# CTBC browser profile & session (contains cookies / binaries — never commit)
brokers/.ctbc_profile/
brokers/.ctbc_session.json
